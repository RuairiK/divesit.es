angular.module('divesitesApp').controller('MainController',
  function($scope) {
    // This is a dummy main controller in case we need it.
    // Using $rootScope allows us to broadcast events to siblings
    // but we might eventually want to be more sophisticated than
    // that.
  }
);
